version: "3"
services:
  iploc:
    build:
      context: .
      dockerfile: Dockerfile
    image: iploc:latest
    container_name: "iploc"
    restart: always
    ports:
      - "80:80"
      - "443:443"
    environment:
      - TOKEN
      - SSL
      - URL